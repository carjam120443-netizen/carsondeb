# CarsonDEB live user configuration.
# live-config reads these variables during first boot and creates the
# autologin user for the live desktop session.
LIVE_USERNAME="carson"
LIVE_USER_FULLNAME="CarsonDEB User"
LIVE_USER_DEFAULT_GROUPS="audio,cdrom,dip,floppy,video,plugdev,netdev,powerdev,scanner,bluetooth,sudo"
