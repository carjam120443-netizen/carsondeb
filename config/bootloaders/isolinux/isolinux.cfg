# CarsonDEB BIOS/legacy boot menu configuration.
# live-build fills the menu entries into the generated configuration.

include menu.cfg

default vesamenu.c32
prompt 0
timeout 50

# CarsonDEB uses the bundled live-build menu/theme files while replacing
# the default Debian timeout/branding behavior with a short visible menu.
